apiVersion: v1
kind: ServiceAccount
metadata:
  name: tap-rag
  labels:
    app: tap-rag-api
  # Set via: kustomize edit set annotation serviceaccount/tap-rag \
  #   eks.amazonaws.com/role-arn=arn:aws:iam::ACCOUNT:role/tap-rag-platform-dev-ecs-task
