apiVersion: apps/v1
kind: Deployment
metadata:
  name: tap-rag-api
  labels:
    app: tap-rag-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tap-rag-api
  template:
    metadata:
      labels:
        app: tap-rag-api
    spec:
      serviceAccountName: tap-rag
      containers:
        - name: api
          image: tap-rag-api:latest
          ports:
            - containerPort: 8000
          env:
            - name: APP_ENV
              value: production
            - name: USE_MOCK_LLM
              value: "false"
            - name: USE_MOCK_EMBEDDINGS
              value: "false"
            - name: AWS_REGION
              value: us-east-1
            - name: CHROMA_PERSIST_DIR
              value: /data/chroma
            - name: CHROMA_S3_SYNC
              value: "true"
            - name: S3_BUCKET
              value: tap-rag-platform-dev-artifacts
            - name: REPUTATION_DATA_DIR
              value: /app/data/reputation
            - name: TRAINING_DATA_PATH
              value: /app/data/training/tap_training_data.json
            - name: API_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tap-rag-api
                  key: API_AUTH_TOKEN
                  optional: true
          volumeMounts:
            - name: chroma
              mountPath: /data/chroma
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
      # emptyDir + S3 hydrate on boot (CHROMA_S3_SYNC). RWO PVC cannot attach to 2 replicas.
      volumes:
        - name: chroma
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: tap-rag-api
spec:
  selector:
    app: tap-rag-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tap-rag-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tap-rag-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
